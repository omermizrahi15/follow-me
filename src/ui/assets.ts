import type { ImageSourcePropType } from 'react-native';

/* eslint-disable @typescript-eslint/no-var-requires, @typescript-eslint/no-require-imports */
/** App logo — camera + mountains silhouette (navy with white peaks on white). */
export const logoSource = require('../../assets/logo.png') as ImageSourcePropType;
